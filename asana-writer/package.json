{
  "name": "asana-writer-skill",
  "version": "1.0.0",
  "private": true,
  "description": "Command-line helpers for safe, explicit Asana writes",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check lib/*.js && node --check scripts/*.js"
  },
  "dependencies": {
    "asana": "3.1.12"
  }
}
